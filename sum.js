//sum of all numbers in an array
let arr=function(sum)
{
    let add=0
    let len =sum.length;
    for(i=0;i<len;i++)
    {
add=add+sum[i]
    }
    console.log(add)
}

let a=[1,2,3,4,5,6,7,8,9,6,5]
arr(a)